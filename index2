<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>noizzed | Territory & Laboratory</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #000000;
            --text: #ffffff;
            --accent: #00ff41; /* Verde Técnico */
            --border: 1px solid #ffffff;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg);
            color: var(--text);
            font-family: 'Space Mono', monospace;
            line-height: 1.2;
            overflow-x: hidden;
        }

        /* Efeito de scanline (Monitor CRT) */
        body::before {
            content: " ";
            display: block; position: fixed; top: 0; left: 0; bottom: 0; right: 0;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), 
                        linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            z-index: 1000; background-size: 100% 4px, 3px 100%; pointer-events: none;
        }

        /* Glitch Logo */
        .glitch { position: relative; animation: glitch 2.5s linear infinite; }
        @keyframes glitch {
            2%, 64% { transform: translate(2px,0) skew(0deg); }
            4%, 60% { transform: translate(-2px,0) skew(0deg); }
            62% { transform: translate(0,0) skew(5deg); }
        }

        header {
            padding: 1rem 2rem; border-bottom: var(--border); display: flex;
            justify-content: space-between; align-items: center;
            position: sticky; top: 0; background: var(--bg); z-index: 999;
        }

        .logo { font-size: 2.2rem; font-weight: 700; text-transform: lowercase; letter-spacing: -2px; }

        nav a {
            color: var(--text); text-decoration: none; margin-left: 20px;
            font-size: 0.8rem; border: 1px solid transparent; padding: 5px 10px;
            text-transform: uppercase;
        }
        nav a:hover { border: 1px solid var(--accent); color: var(--accent); }

        /* PLATFORMS NAV */
        .platforms-nav {
            display: flex; padding: 10px 2rem; border-bottom: var(--border);
            gap: 15px; background: #080808; overflow-x: auto;
        }
        .platforms-nav a { color: #888; text-decoration: none; font-size: 10px; text-transform: uppercase; letter-spacing: 1px; white-space: nowrap; }
        .platforms-nav a:hover { color: var(--accent); text-decoration: underline; }

        .section-title {
            padding: 1.2rem 2rem; background: var(--text); color: var(--bg);
            font-weight: bold; text-transform: uppercase; letter-spacing: 2px;
            border-bottom: var(--border);
        }

        /* HERO FLEX (Lado a Lado) */
        .hero-flex { display: flex; border-bottom: var(--border); min-height: 450px; }
        .hero-left {
            flex: 1; padding: 4rem 2rem; display: flex; flex-direction: column;
            justify-content: center; border-right: var(--border);
        }
        .hero-right {
            flex: 1; background: #111; display: flex; align-items: center; justify-content: center; overflow: hidden;
        }
        .hero-right img { width: 100%; height: 100%; object-fit: cover; filter: grayscale(100%); transition: 0.5s; }
        .hero-right img:hover { filter: grayscale(0%); }

        h1 { font-size: 8vw; line-height: 0.8; text-transform: lowercase; word-break: break-all; }
        .hero-left p { font-size: 1.2rem; color: var(--accent); margin-top: 1rem; }

        /* MANIFESTO */
        .manifesto { display: grid; grid-template-columns: 1fr 1fr; border-bottom: var(--border); }
        .manifesto-box { padding: 4rem 2rem; border-right: var(--border); font-size: 1.2rem; text-transform: uppercase; }
        .manifesto-box.highlight { background-color: var(--accent); color: var(--bg); font-weight: bold; }

        /* CREDENTIALS */
        .credentials { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); border-bottom: var(--border); }
        .cred-item { padding: 2rem; border-right: var(--border); }
        .cred-item h3 { color: var(--accent); margin-bottom: 1rem; font-size: 0.9rem; }

        /* CONTENT CONTAINERS */
        .audio-container { padding: 3rem 2rem; border-bottom: var(--border); background: #050505; }
        
        .discog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); gap: 15px; padding: 2rem; border-bottom: var(--border); }
        .discog-item img { width: 100%; border: 1px solid #333; transition: 0.3s; filter: grayscale(100%); }
        .discog-item img:hover { filter: grayscale(0%); border-color: var(--accent); }

        .press-list { padding: 2rem; border-bottom: var(--border); }
        .press-list li { list-style: none; margin-bottom: 1rem; }
        .press-list a { color: var(--text); text-decoration: none; border-bottom: 1px dotted #555; }

        /* CONTACT */
        .contact { padding: 6rem 2rem; text-align: center; border-bottom: var(--border); }
        .btn {
            display: inline-block; padding: 2rem 4rem; border: var(--border);
            color: var(--text); text-decoration: none; font-size: 2rem; font-weight: bold;
            transition: 0.3s;
        }
        .btn:hover { background: var(--accent); color: var(--bg); transform: translateY(-5px); }

        /* GALLERY */
        .visual-archive { background: #000; border-bottom: var(--border); position: relative; }
        .carousel-wrapper { overflow: hidden; position: relative; width: 100%; }
        .carousel-track { display: flex; transition: transform 0.6s cubic-bezier(0.23, 1, 0.32, 1); }
        .slide { min-width: 100%; height: 500px; display: flex; align-items: center; justify-content: center; overflow: hidden; }
        .slide img { width: 100%; height: 100%; object-fit: cover; filter: grayscale(100%) contrast(110%); }
        
        .carousel-nav { position: absolute; bottom: 20px; right: 20px; display: flex; gap: 10px; z-index: 100; }
        .nav-btn { background: var(--bg); color: var(--text); border: var(--border); padding: 15px 25px; cursor: pointer; font-family: 'Space Mono'; }
        .nav-btn:hover { background: var(--accent); color: var(--bg); }

        footer { padding: 3rem 2rem; text-align: center; font-size: 0.8rem; opacity: 0.6; }

        @media (max-width: 850px) {
            h1 { font-size: 15vw; }
            .hero-flex { flex-direction: column; height: auto; }
            .hero-right { height: 300px; border-top: var(--border); }
            .manifesto { grid-template-columns: 1fr; }
            .manifesto-box { border-right: none; border-bottom: var(--border); }
            .btn { font-size: 1.2rem; padding: 1.5rem; width: 100%; }
            .platforms-nav { padding: 10px; }
        }
    </style>
</head>
<body>

    <!-- WHATSAPP FLOAT -->
    <a href="https://wa.me/55SEUNUMERO?text=Ola%20vim%20pelo%20site" target="_blank" style="position: fixed; bottom: 20px; right: 20px; background: #25D366; color: #fff; padding: 15px; border-radius: 50%; font-size: 2rem; text-decoration: none; z-index: 9999; border: 2px solid #000;">💬</a>

    <header>
        <div class="logo glitch">noizzed</div>
        <nav>
            <a href="#lab">LAB</a>
            <a href="#ensino">ENSINO</a>
            <a href="#booking">BOOKINGS</a>
        </nav>
    </header>

    <div class="platforms-nav">
        <!-- 🔴 LINKS IMPORTANTES: PREENCHA AQUI -->
        <a href="https://www.beatport.com/" target="_blank">beatport</a>
        <a href="#" target="_blank">traxsource</a>
        <a href="#" target="_blank">spotify</a>
        <a href="#" target="_blank">soundcloud</a>
        <a href="#" target="_blank">bandcamp</a>
        <a href="#" target="_blank">youtube</a>
        <a href="https://instagram.com/noizzedofficial" target="_blank">instagram</a>
    </div>

    <section class="hero-flex">
        <div class="hero-left">
            <h1>noizzed</h1>
            <p>[ Pista é território. Cabine é laboratório. ]</p>
            
            <!-- PRESS KIT DOWNLOAD -->
            <div style="margin-top: 2rem; border: 1px dashed #00ff41; padding: 1rem; display: inline-block;">
                <p style="font-size: 0.7rem; margin-bottom: 5px; color: #888;">[ PROMOTER AREA ]</p>
                <a href="LINK_DO_DRIVE" target="_blank" style="background: #00ff41; padding: 8px 15px; text-decoration: none; font-weight: bold; color: #000; font-size: 0.8rem;">
                    DOWNLOAD PRESS KIT
                </a>
            </div>
        </div>
        <div class="hero-right">
            <!-- 🔴 FOTO PRINCIPAL (Suba no GitHub e coloque o nome aqui) -->
            <!-- Usei uma provisória, troque pela sua -->
            <img src="https://images.unsplash.com/photo-1571266028243-e4733b0f0bb1?q=80&w=2070" alt="Noizzed Hero">
        </div>
    </section>

    <section class="manifesto">
        <div class="manifesto-box">
            noizzed não repete sets. lê a pista, sente o espaço e cria ao vivo.<br><br>
            > Autorais, clássicos, loops e samples viram música nova.
        </div>
        <div class="manifesto-box highlight">
            CEO da ESTUFA RECORDS.<br>
            Fundadora do coletivo TPM (#todaspelamusica).<br>
            Quebrando estruturas e hackeando o sistema.
        </div>
    </section>

    <div class="section-title">Credenciais & Log de Suporte</div>
    <section class="credentials">
        <div class="cred-item">
            <h3>GLOBAL SUPPORT</h3>
            <p>Richie Hawtin<br>Peggy Gou<br>Paco Osuna</p>
        </div>
        <div class="cred-item">
            <h3>RELEASES</h3>
            <p>Estufa Records<br>Tantsa Records<br>Letrux (Remix Oficial)</p>
        </div>
        <div class="cred-item">
            <h3>SOUNDSCAPE</h3>
            <p>Detroit, Euro, Trance & Breaks. Controle e risco em tempo real.</p>
        </div>
    </section>

    <div class="section-title">Visual_Feed // Live_Action</div>
    <section style="display: flex; flex-wrap: wrap; background: #000; border-bottom: var(--border);">
        <!-- 🔴 SEUS VIDEOS DO YOUTUBE AQUI -->
        <div style="flex: 1 1 400px; height: 350px;">
            <iframe width="100%" height="100%" src="https://www.youtube.com/embed/SEU_VIDEO_ID" frameborder="0" allowfullscreen></iframe>
        </div>
        <div style="flex: 1 1 400px; height: 350px;">
            <iframe width="100%" height="100%" src="https://www.youtube.com/embed/SEU_VIDEO_ID_2" frameborder="0" allowfullscreen></iframe>
        </div>
    </section>

    <div class="section-title" id="lab">Audio_Data // Discography</div>
    <section class="discog-grid">
        <!-- 🔴 CAPAS DOS ALBUNS AQUI -->
        <div class="discog-item"><img src="https://via.placeholder.com/150/000000/00ff41?text=EP+01"></div>
        <div class="discog-item"><img src="https://via.placeholder.com/150/000000/00ff41?text=ALBUM"></div>
        <div class="discog-item"><img src="https://via.placeholder.com/150/000000/00ff41?text=RMX"></div>
        <div class="discog-item"><img src="https://via.placeholder.com/150/000000/00ff41?text=SGLE"></div>
    </section>

    <section class="audio-container">
        <!-- 🔴 SOUNDCLOUD -->
        <iframe width="100%" height="166" scrolling="no" frameborder="no" allow="autoplay" src="https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/playlists/1326902578&color=%2300ff41&auto_play=false&hide_related=false&show_comments=true&show_user=true&show_reposts=false&show_teaser=true"></iframe>
    </section>

    <div class="section-title" id="ensino">Press_Log // Mídia</div>
    <section class="press-list">
        <ul>
            <li><span style="color:var(--accent)">[MIXMAG]</span> <a href="#">"Ousadia sonora e sets inusitados: conheça noizzed"</a></li>
            <li><span style="color:var(--accent)">[HOUSE MAG]</span> <a href="#">"Uma mulher à frente de sua própria estufa"</a></li>
        </ul>
    </section>

    <section class="contact" id="booking">
        <p style="margin-bottom: 2.5rem; letter-spacing: 5px; opacity: 0.7;">INICIAR PROTOCOLO DE CONTRATAÇÃO</p>
        <a href="mailto:contato@noizzed.xyz" class="btn">BOOKING_NOW</a>
    </section>

    <div class="section-title">Visual_Archive // Galeria</div>
    <section class="visual-archive">
        <div class="carousel-wrapper">
            <div class="carousel-track" id="track">
                <!-- 🔴 FOTOS DA GALERIA -->
                <div class="slide"><img src="https://images.unsplash.com/photo-1598387181032-a3103a2db5b3?q=80&w=2076" alt="Crowd"></div>
                <div class="slide"><img src="https://images.unsplash.com/photo-1470225620780-dba8ba36b745?q=80&w=2070" alt="Dj Set"></div>
            </div>
        </div>
        <div class="carousel-nav">
            <button class="nav-btn" onclick="moveSlide(-1)"> < </button>
            <button class="nav-btn" onclick="moveSlide(1)"> > </button>
        </div>
    </section>	

    <footer>
        NOIZZED // ALL RIGHTS RESERVED // STO-BRAZIL-2026
    </footer>

    <script>
        let currentIndex = 0;
        const track = document.getElementById('track');
        const slides = document.querySelectorAll('.slide');

        function moveSlide(direction) {
            currentIndex += direction;
            if (currentIndex < 0) currentIndex = slides.length - 1;
            if (currentIndex >= slides.length) currentIndex = 0;
            const offset = currentIndex * -100;
            track.style.transform = `translateX(${offset}%)`;
        }
    </script>
</body>
</html>
